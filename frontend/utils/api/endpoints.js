// eTRONs API endpoints

const WORKSPACE_BASE_URL = "https://t8mhrt9a61.execute-api.ap-southeast-2.amazonaws.com/Prod";

const endpoints = {
    workspace: {
        create: `${WORKSPACE_BASE_URL}/workspace`
    }
}

export default endpoints;