/*module.exports = {
    transform: {
      "^.+\\.mjs$": "babel-jest",  // Use babel-jest to transform mjs files
      "^.+\\.js$": "babel-jest",   // Use babel-jest for js files as well
    },
    moduleFileExtensions: ['js', 'mjs', 'json', 'node'],  // Allow both .js and .mjs
    testEnvironment: 'node',  // Set test environment to node (needed for Lambda functions)
  };*/
  